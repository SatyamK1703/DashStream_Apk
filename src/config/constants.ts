// src/config/constants.ts

// API URL for direct access
export const API_URL = 'https://dash-stream-apk-backend.vercel.app/api'; // Production API URL

// Re-export API_CONFIG from constants/config.ts for consistency
export { API_CONFIG, APP_CONFIG } from '../constants/config';